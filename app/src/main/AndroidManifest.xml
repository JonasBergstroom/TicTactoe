<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tictactoe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DrawBotPlay"></activity>
        <activity android:name=".Exit" />
        <activity android:name=".botPlayer_wins" />
        <activity android:name=".Player_Wins" />
        <activity android:name=".BotPlay" />
        <activity android:name=".Draw" />
        <activity android:name=".player2_wins" />
        <activity android:name=".Player1_Wins" />
        <activity android:name=".MainActivity" />
        <activity android:name=".welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>